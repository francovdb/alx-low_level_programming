#include "main.h"
/**
 * _abs - Entry Point
 * computes absolute value of integer
 * @n: computed integer
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
